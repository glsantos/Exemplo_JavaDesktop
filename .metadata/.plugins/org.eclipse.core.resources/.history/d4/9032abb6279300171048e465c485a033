
public class ContaPremium extends Conta{
	
	private Double limite=500d;
	
	@Override
	public void visualizarSaldo() {
		
		System.out.println("O saldo disponível é:" + saldo);
		System.out.println("O limite disponivel é: "+ limite);
		
		visualizarSaldo();
	}
	
	
	@Override
	public Double saque(Double valor){
		
		if(valor > saldo){
			
			Double diferenca = valor - saldo;
			
			if(limite >= diferenca){
				
				saldo=  0d;
				limite -= diferenca;
				return valor;
			}else{
				
				return 0d;
			}
		}else{
			
			super.saque(valor);
		}
		
		return 0d;
	}
}
